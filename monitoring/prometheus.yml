global:
  scrape_interval: 15s

scrape_configs:
  # Local FastAPI backend running on the host machine
  - job_name: "backend-local"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:8000"]

